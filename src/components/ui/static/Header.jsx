/**
 * @jsx React.DOM
 */

var React = require('react');

var Header = React.createClass({

    render: function() {
        return (
            <header>
                This is the header
            </header>
        );
    }

});

module.exports = Header;