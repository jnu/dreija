/**
 * @jsx React.DOM
 */

var React = require('react');

var Footer = React.createClass({

    render: function() {
        return (
            <footer>
                This is the footer
            </footer>
        );
    }

});

module.exports = Footer;